# Sentient Web Development Dockerfile
# Uses Vite dev server with HMR

FROM node:20-alpine

# Install development tools
RUN apk add --no-cache git curl

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies
RUN npm install

# Expose Vite dev server port
EXPOSE 4002

# Start Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "4002"]